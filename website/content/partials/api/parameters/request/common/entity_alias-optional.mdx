<a id="id-request" />

**`id`** `<string>: ""` <b style={{color: 'var(--gray-3)'}}>optional</b>

ID of the client token, specified by a root token. When `id` is empty, Vault
assigns a randomly generated value. Token IDs cannot:

- contain the dot character (`.`)
- start with `s.`