<a id="env-vault_client_cert" />

- **`VAULT_CLIENT_CERT`** - Path to a PEM-encoded CA certificate file on the
  local disk. Used for TLS communication with the server.
  
  ```shell-session
  $ export VAULT_CACERT="/path/to/certs/mycert.pem"
  ```
