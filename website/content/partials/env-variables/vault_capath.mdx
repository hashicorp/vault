<a id="env-vault_capath" />

- **`VAULT_CAPATH`** - Path to a directory with PEM-encoded CA certificate files
  on the local disk. Used to verify SSL certificates for the server.
  
  ```shell-session
  $ export VAULT_CAPATH="/path/to/certs/"
  ```