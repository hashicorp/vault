<a id="env-vault_cli_no_color" />

- **`VAULT_CLI_NO_COLOR`** - Tell Vault not to use ANSI color escape sequence
  characters in terminal output.
  
  ```shell-session
  $ export VAULT_CLI_NO_COLOR="TRUE"
  ```
