As of Vault 1.20+ent and later, `/sys/internal/counters/activity` API will not work for custom contracts (non-yearly billing cycles). Please use activity export API `/sys/internal/counters/activity/export` to get client usage information.