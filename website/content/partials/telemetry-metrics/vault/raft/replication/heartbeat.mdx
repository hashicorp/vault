### vault.raft.replication.heartbeat ((#vault-raft-replication-heartbeat))

Metric type | Value | Description
----------- | ----- | -----------
timer       | ms    | Time required to invoke `appendEntries` on a peer so the peer does not time out
