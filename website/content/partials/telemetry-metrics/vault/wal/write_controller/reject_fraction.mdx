### vault.wal.write_controller.reject_fraction ((#vault-wal-write_controller-reject_fraction))

Metric type | Value   | Description
----------- | ------- | -----------
gauge       | number  | Fraction between 0 and 1 indicating the number of write requests the [write controller](/vault/docs/concepts/adaptive-overload-protection) currently estimate need to be rejected to maintain cluster stability.