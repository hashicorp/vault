### vault.core.replication.performance.primary ((#vault-core-replication-performance-primary))

Metric type | Value   | Description
----------- | ------- | -----------
gauge       | boolean | Indicates whether the Vault node is a performance primary

- A value of `1` indicates that the node **is** a performance primary.
- A value of `0` indicates that the node **is not** a performance primary.