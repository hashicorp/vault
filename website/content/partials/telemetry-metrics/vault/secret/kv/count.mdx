### vault.secret.kv.count ((#vault-secret-kv-count))

Metric type | Value   | Description
----------- | ------- | -----------
gauge       | integer | Number of entries in each key-value secrets engines

Vault organizes the key-value pair count by cluster, namespace, and mount point.