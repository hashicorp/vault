<a id="pki-arg-parent" />

**`parent (string : <required>)`**

The fully qualified path to the Certificate Authority (CA) in Vault that will
sign and issue the new intermediate certificate. This CA will become the
parent of the newly created intermediate certificate.

**Example**: `/pki_root/issuer/default`