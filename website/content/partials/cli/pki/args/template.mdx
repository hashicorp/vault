<a id="pki-arg-template" />

**`template (string : <required>)`**

The fully qualified path of an intermediate certificate in Vault which will be
used to populate certificate fields not overridden by `[options]`.

**Example**: `/pki_int/issuer/FirstDepartment`