<a id="pki-arg-issuer" />

**`issuer (string : <required>)`**

The fully name-spaced path to the issuer certificate which will be
used to verify the `<issued>` certificate.

**Example**: `ns1/mount1/issuer/issuerName/json`