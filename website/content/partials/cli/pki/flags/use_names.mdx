<a id="pki-arg-use_names" />

**`-use_names (boolean : false)`**

Whether the list of issuers returned is referred to by name (when it
exists) rather than by issuer_id (uuid).

**Example**: `-use_names`