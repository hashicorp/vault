<a id="pki-arg-default-disabled" />

**`default_disabled (boolean : false)`**

When specified, results in all health checks being disabled by default unless 
enabled by the configuration file explicitly. By default, all default-enabled 
health checks will run.

**Example**: `-default_disabled`