<a id="shared-arg-mount_path" />

**`mount_path (string : <required>)`**

The internal path where Vault can access an enabled plugin. Mount paths must be
unique across all plugins.

**Example**: `shared/`
