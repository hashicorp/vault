<a id="shared-flag-field" />

**`-field (string : "")`**

Limits output to the named output field. CLI results that use `-field` do not
include trailing newlines to simplify piping the output to other processes.

**Example**: `-field "expiry_date"`
