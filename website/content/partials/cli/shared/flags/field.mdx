<a id="shared-flag-field" />

**`-field (string : "")`**

Limit CLI output to a specific field from the response data. The `-field` flag
takes precedence over other formatting directives and prints the result without
a trailing newline.

**Example**: `-field "id"`
