<a id="operator-arg-key" />

**`key (string : "")`**

The unseal key to use. If key is specified as `-`, the command will read from 
stdin. If a TTY is available, the command will prompt for text.

**Example**: `s6jZ4zQ5Z8zY6xQ6Z6xQ`