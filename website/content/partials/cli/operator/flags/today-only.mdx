<a id="operator-flag-today-only" />

**`-today-only (boolean : false)`**

Include only today's snapshot, no historical snapshots. If no snapshots were 
persisted in the last 24 hrs, it takes a snapshot and exports it to a bundle.

**Example**: `-today-only`
