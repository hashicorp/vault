<a id="operator-flag-key-shares" />

**`-key-shares (integer : 5)`**

Number of key shares to split the generated master key into. This is the
number of "unseal keys" or "recovery keys" to generate. This is aliased as `-n`.

**Example**: `-key-shares 5`
