<a id="operator-flag-reset" />

**`-reset (boolean : false)`**

Reset the migration lock. A lock file is added during migration to prevent
starting the Vault server or another migration. You can use the `-reset` flag
to remove a stale lock file if present.

**Example**: `-reset`
