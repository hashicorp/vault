<a id="operator-flag-generate-otp" />

**`-generate-otp (boolean : false)`**

Generate and print a high-entropy one-time-password (OTP) suitable for use with 
the `-init` flag.

**Example**: `-generate-otp`
