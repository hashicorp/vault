<a id="operator-flag-target" />

**`-target (string : "barrier")`**

Target for rekeying. The valid values are `"barrier"` and `"recovery"`. The 
`"recovery"` target only applies when HSM support is enabled.

**Example**: `-target "recovery"`
