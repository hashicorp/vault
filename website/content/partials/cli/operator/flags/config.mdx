<a id="operator-flag-config" />

**`-config (string : "")`**

Path to a Vault configuration file or directory of configuration files. This flag 
can be specified multiple times to load multiple configurations. If the path is a 
directory, all files which end in `.hcl` or `.json` are loaded.

**Example**: `-config "path/to/config.hcl"`
