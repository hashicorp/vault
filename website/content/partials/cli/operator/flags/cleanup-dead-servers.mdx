<a id="operator-flag-cleanup-dead-servers" />

**`-cleanup-dead-servers (boolean : false)`**

Controls whether to remove dead servers from the Raft peer list periodically or
when a new server joins. This requires that `min-quorum` is also set.

**Example**: `-cleanup-dead-servers`
