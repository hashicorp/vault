<a id="operator-flag-nonce" />

**`-nonce (string : "")`**

Nonce value returned at initialization. You must provide the same nonce value 
with each unseal or recovery key. Only needed when providing an unseal or 
recovery key.

**Example**: `-nonce "foo"`
