<a id="ssh-flag-public-key-path" />

**`-public-key-path (string : "~/.ssh/id_rsa.pub")`**

Path to the SSH public key to send to Vault for signing.

**Example**: `-private-key-path ~/.ssh/custom_key`