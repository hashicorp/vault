<a id="ssh-flag-private-key-path" />

**`-private-key-path (string : "~/.ssh/id_rsa")`**

Path to the SSH private key to use for authentication. This must be the
corresponding private key to `-public-key-path`.

**Example**: `-private-key-path ~/.ssh/custom_key`