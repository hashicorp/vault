<a id="plugin-flag-rootless" />

**`-rootless (bool : false)`**

Whether the container runtime is configured to run as a non-privileged
(non-root) user. Required if the plugin container image is also configured to
run as a non-root user.

**Example**: `-rootless`
