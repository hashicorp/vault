<a id="plugin-flag-oci-image" />

**`-oci_image (string : "")`**

OCI image to run. If specified, setting `command`, `args`, and `env` will update 
the container's entrypoint, args, and environment variables (append-only)
respectively.

**Example**: `-oci_image "example-secret-plugin-oci-image"`
