<a id="plugin-flag-scope" />

**`scope (string : "")`**

The scope of the reload. For local reloads, omit this flag. For reloads that
span multiple Vault clusters, use `global`.

**Example**: `-scope "global"`
