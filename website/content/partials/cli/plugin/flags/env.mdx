<a id="plugin-flag-env" />

**`-env (string : "")`**

Environment variables to set for the plugin when starting. You can specify this
flag multiple times to set multiple environment variables.

**Example**: `-env "TOKEN_FILE=/var/run/token"`
