<a id="plugin-flag-command" />

**`-command (string : "")`**

Command to spawn the plugin. This defaults to the name of the plugin if both
`oci_image` and `command` are unspecified.

**Example**: `-command "my-secret-plugin-1.0.1"`
