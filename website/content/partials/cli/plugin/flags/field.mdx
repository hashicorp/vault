<a id="plugin-flag-field" />

**`-field (string : "")`**

Print only the field with the given name. Specifying this option will take
precedence over other formatting directives. The result will not have a trailing
newline making it ideal for piping to other processes.

**Example**: `-field "name"`
