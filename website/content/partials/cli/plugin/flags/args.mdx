<a id="plugin-flag-args" />

**`-args (string : "")`**

Argument to pass to the plugin when starting. You can specify this flag multiple 
times to add multiple arguments.

**Example**: `-args "--with-glibc"`
