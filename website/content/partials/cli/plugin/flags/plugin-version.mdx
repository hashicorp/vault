<a id="plugin-flag-plugin-version" />

**`plugin-version (string : "")`**

Semantic version of the plugin to read from the catalog. If unspecified, refers
to the unversioned plugin registered with the same name and type, or the
built-in plugin, in that order of precedence.

**Example**: `-plugin-version "v1.0.1"`
