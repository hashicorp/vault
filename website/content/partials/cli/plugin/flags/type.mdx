<a id="plugin-flag-type" />

**`-type (string : "")`**

The type of plugin to reload. Must be `"auth"`, `"secret"`, or `"database"`.
Mutually exclusive with `-mounts`. If not provided, all plugins with a matching
name will be reloaded.

**Example**: `-type "auth"`
