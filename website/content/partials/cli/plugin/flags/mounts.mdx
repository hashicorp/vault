<a id="plugin-flag-mounts" />

**`mounts (string : "")`**

Array or comma-separated string of mount paths of the plugin backends to
reload.

**Example**: `-mounts "auth/my-custom-plugin-1,auth/my-custom-plugin-2"`
