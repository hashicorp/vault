<a id="plugin-flag-register-version" />

**`version (string : "")`**

Semantic version of the plugin. Used as the tag when specifying `-oci_image`, 
but Vault will automatically trim any leading 'v' from the version.

**Example**: `-version "v1.0.1"`
