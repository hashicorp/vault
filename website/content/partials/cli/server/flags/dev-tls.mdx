<a id="server-flag-dev-tls" />

**`-dev-tls (bool: false)`**

Enable TLS in `dev` mode. In this mode, Vault runs in-memory and starts
unsealed with a generated TLS CA, certificate and key. As the name implies, do
not run "dev" mode in production.

**Example**: `-dev-tls`
