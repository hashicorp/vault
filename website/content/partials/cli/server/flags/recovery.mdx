<a id="server-flag-recovery" />

**`-recovery (bool: false)`**

Enable recovery mode. Vault is used to perform recovery actions. Using a
recovery token, you can use the "sys/raw" API to manipulate the storage.

**Example**: `-recovery`
