<a id="server-flag-dev" />

**`-dev (bool: false)`**

Enable development mode. In `dev` mode, Vault runs in-memory and starts
unsealed. As the name implies, do not run "dev" mode in production.

**Example**: `-dev`