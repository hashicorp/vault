<a id="server-flag-dev-tls-cert-dir" />

**`-dev-tls-cert-dir (string: "")`**

Directory where generated TLS files are created if `-dev-tls` is specified. If
left unset, files are generated in a temporary directory.

**Example**: `-dev-tls-cert-dir /path/to/directory`
