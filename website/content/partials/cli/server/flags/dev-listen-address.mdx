<a id="server-flag-dev-listen-address" />

**`-dev-listen-address (string: "127.0.0.1:8200")`**

Address to bind to in `dev` mode. You can also specify the port via the
`VAULT_DEV_LISTEN_ADDRESS` environment variable.

**Example**: `-dev-listen-address 127.0.0.1:8200`
