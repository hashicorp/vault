<a id="server-flag-log-rotate-bytes" />

**`-log-rotate-bytes (int : 0)`**

The number of bytes that should be written to a log before it needs to be rotated.
Unless specified, there is no limit to the number of bytes that can be written to
a log file.

**Example**: `-log-rotate-bytes 1048576`