<a id="server-flag-log-rotate-duration" />

**`-log-rotate-duration (duration : 24h)`**

The maximum duration a log should be written to before it needs to be rotated.
Must be a duration value such as 30s.

**Example**: `-log-rotate-duration 30m`