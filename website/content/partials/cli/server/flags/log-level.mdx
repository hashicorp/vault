<a id="server-flag-log-level" />

**`-log-level (string : "info")`**

Log verbosity level. Supported values (in order of descending detail) are
`trace`, `debug`, `info`, `warn`, and `error`. You can also specify this via
the `VAULT_LOG_LEVEL` environment variable.

**Example**: `-log-level info`