<a id="server-flag-log-format" />

**`-log-format (string : "standard")`**

Log format. Supported values are `standard` and `json`. You can also specify
this via the `VAULT_LOG_FORMAT` environment variable.

**Example**: `-log-format json`