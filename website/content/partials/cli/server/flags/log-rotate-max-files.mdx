<a id="server-flag-log-rotate-max-files" />

**`-log-rotate-max-files (int : 0)`**

The maximum number of older log file archives to keep. Defaults to 0 (no files
are ever deleted). Set to -1 to discard old log files when a new one is created.

**Example**: `-log-rotate-max-files 10`