<a id="secrets-flag-listing-visibility" />

**`-listing-visibility (string : "")`**

The flag to toggle whether to show the mount in the UI-specific listing endpoint. 
Valid values are `"unauth"` or `"hidden"`, with the default `""` being 
equivalent to `"hidden"`.

**Example**: `-listing-visibility "unauth"`
