<a id="secrets-flag-default-lease-ttl" />

**`-default-lease-ttl (string : "")`**

The default lease TTL for this secrets engine. If unspecified, this defaults
to the Vault server's globally configured default lease TTL. Uses 
[duration format strings](/vault/docs/concepts/duration-format).

**Example**: `-default-lease-ttl "30m"`