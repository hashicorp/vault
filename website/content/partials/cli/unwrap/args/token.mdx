<a id="unwrap-arg-token" />

**`token (string : "")`**

The token containing the wrapped secret to unwrap. If not provided, the command will 
unwrap the secret stored in the currently authenticated token.

**Example**: `3de9ece1-b347-e143-29b0-dc2dc31caafd`