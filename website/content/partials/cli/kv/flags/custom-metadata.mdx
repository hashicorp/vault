<a id="kv-flag-custom-metadata" />

**`-custom-metadata (string : "")`**

Specifies arbitrary version-agnostic `key=value` metadata meant to
describe a secret. You can specify this flag multiple times to add multiple
pieces of metadata.

**Example**: `-custom-metadata "foo=bar"`