<a id="kv-flag-method" />

**`-method (enum : patch)`**

The preferred patch method.

Value   | Description
------- | -----------
`patch` | Use an HTTP `PATCH` request
`rw`    | Combine an HTTP `GET` request, local update, and HTTP `POST` request

**Example**: `-method rw`