<a id="kv-flag-cas-required" />

**`-cas-required (boolean : false)`**

If true, the key will require the `cas` parameter to be set on all write 
requests. By default, the backend's configuration will be used.

**Example**: `-cas_required`