<a id="kv-flag-cas-required" />

**`-cas-required (boolean : false)`**

Override the `cas_required` field in the plugin configuration to require a
check-and-set operations on all write requests. Setting `cas-required` to `true`
makes the `-cas` flag required.

**Example**: `-cas_required`