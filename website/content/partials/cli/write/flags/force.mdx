<a id="write-flag-force" />

**`-force (bool: false)`**

Allow the operation to continue with no `key=value` pairs. This lets you write 
keys that do not need or expect data. This is aliased as `-f`.

**Example**: `-force`