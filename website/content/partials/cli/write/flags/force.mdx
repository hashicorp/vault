<a id="write-flag-force" />

**`[-force | -f] (bool : false)`**

Forces the CLI command to execute without explicit key/value pairs when writing
to keys that do not need or expect data.

**Example**: `-force`
