---

<a id="arg-filter" />

**`filter=<string: "">`** <EnterpriseAlert inline="true" />

Define a filter string for the audit device. For more information, see 
[`filter` properties for audit devices](/vault/docs/enterprise/audit/filtering#filter-syntax-for-audit-results).

```text
filter="namespace == \"admin/\""
```
