<a id="token-accessor" />

**`-accessor (bool : false)`**

Treat the argument as an accessor instead of a token. When this option is 
selected, the output will NOT include the token.

**Example**: `-accessor=false`