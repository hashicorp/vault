<a id="token-metadata" />

`-metadata (k=v: "")`**

Arbitrary key=value metadata to associate with the token. This metadata will show 
in the audit log when the token is used. You can specify this flag multiple times 
to add multiple pieces of metadata.

**Example**: `-metadata=env=dev`