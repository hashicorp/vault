<a id="token-arg-token" />

**`token (string : "")`**

The authentication token to use. If not provided, the command will use the 
locally authenticated token.

**Example**: `96ddf4bc-d217-f3ba-f9bd-017055595017`