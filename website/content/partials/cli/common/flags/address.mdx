---

<a id="flag-address" />

**`-address <string: http://127.0.0.1:8200>`**

Override the `VAULT_ADDR` environment variable to target a specific Vault server
with the command.

```text
-address "http://127.0.10.1:8200"
```