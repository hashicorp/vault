<a id="retry_delay-request" />

**`retry_delay`** `<string: "4s">` <b style={{color: 'var(--gray-3)'}}>optional</b>

The initial delay, in seconds, before the plugin retries an operation. The delay
increases exponentially up to `max_retry_delay`.
