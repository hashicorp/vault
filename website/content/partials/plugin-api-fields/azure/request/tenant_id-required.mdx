<a id="tenant_id-request" />

**`tenant_id`** `<string>` <b style={{color: 'var(--vault-link)'}}>required</b>

<sup>Optional Vault environment variable: <code>AZURE_TENANT_ID</code></sup>

Tenant ID for the Azure Active Directory organization.