<a id="client_id-request" />

**`client_id`** `<string>: ""` <b style={{color: 'var(--gray-3)'}}>optional</b>

<sup>Optional Vault environment variable: <code>AZURE_CLIENT_ID</code></sup>

Client ID of credentials with permission to query compute resources through the
Azure API.