<a id="mount_path-path" />

**`mount_path`** `<string>` <b style={{color: 'var(--vault-link)'}}>required</b>

The mount path in Vault for the enabled plugin.

Plugin type                    | Default path
------------------------------ | ------------
Authentication                 | `auth`
Secret management              | `secrets`
Database credential management | `database`