---
layout: docs
page_title: plugin runtime register - Command
description: |-
  The "plugin runtime register" command registers a new plugin runtime in Vault's plugin runtime
  catalog.
---

# plugin runtime register

Register a new plugin runtime in the plugin runtime catalog.

<CodeBlockConfig hideClipboard>

```shell-session
$ vault plugin runtime register [flags] <name>

$ vault plugin runtime register [-help | -h]
```

</CodeBlockConfig>

Register a new plugin runtime in the plugin runtime catalog of your Vault instance.

If you register a container plugin without referencing a plugin runtime, it will
use [gVisor's](https://gvisor.dev/) OCI runtime `runsc` by default, with no
resource limits. You can use plugin runtimes to configure other OCI-compliant
runtimes such as `runc`. Vault does not come packaged with any supporting
container runtimes, and you must separately install whichever you want to use.

To use a registered plugin runtime, use the `-runtime` option with the
[plugin registration command](/vault/docs/commands/plugin/register). Refer to
[containerized plugins](/vault/docs/plugins/containerized-plugins) for more
details on running external plugins in containers.

Support for runtime types is currently limited to `container`. Support for the
`container` runtime is currently limited to Linux.

<Tip title="Related API endpoints">

RegisterPluginRuntime - [`POST: /sys/plugins/runtimes/catalog/:type/:name`](/vault/api-docs/system/plugins-runtimes-catalog#register-plugin-runtime)

</Tip>

## Command arguments

<br />

@include 'cli/plugin/args/runtime-name.mdx'

## Command options

None.

## Command flags

<br />

@include 'cli/plugin/flags/cgroup_parent.mdx'

<br /><hr /><br />

@include 'cli/plugin/flags/cpu_nanos.mdx'

<br /><hr /><br />

@include 'cli/plugin/flags/memory_bytes.mdx'

<br /><hr /><br />

@include 'cli/plugin/flags/oci_runtime.mdx'

<br /><hr /><br />

@include 'cli/plugin/flags/rootless.mdx'

<br /><hr /><br />

@include 'cli/plugin/flags/runtime-type.mdx'

## Global flags

<br />

@include 'cli/standard-settings/all-standard-flags-but-format.mdx'

## Examples

Register a plugin runtime:

```shell-session
$ vault plugin runtime register -type=container -oci_runtime=runc runc
Success! Registered plugin runtime: runc
```

Register a plugin runtime with resource limits:

```shell-session
$ vault plugin runtime register \
    -type=container \
    -cpu_nanos=100000000 \
    runsc
```