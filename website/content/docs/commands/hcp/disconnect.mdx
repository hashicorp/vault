---
layout: docs
page_title: hcp disconnect - Command
description: |-
  Use "hcp disconnect" command invalidates live sessions to a HCP Vault cluster.
---

# hcp

The "hcp connect" removes any .

By default, the `hcp disconnect` command invalidates live sessions to a HCP Vault cluster.

## Examples

Disconnect from HCP:

```shell-session
$ vault hcp disconnect
```

## Usage

All [standard set of
flags](/vault/docs/commands) are included.
