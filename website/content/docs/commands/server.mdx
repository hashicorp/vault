---
layout: docs
page_title: server - Command
description: |-
  The "server" command starts a Vault server that responds to API requests. By
  default, Vault will start in a "sealed" state. The Vault cluster must be
  initialized before use.
---

# server

Start a Vault server that responds to API requests.

<CodeBlockConfig hideClipboard>

```shell-session
$ vault server [flags]

$ vault server [-help | -h]
```

</CodeBlockConfig>

## Description

The `server` command starts a Vault server that responds to API requests. By
default, Vault will start in a "sealed" state. You must initialize the Vault
cluster before using it, usually by the `vault operator init` command. You must
also unseal the Vault cluster using the `vault operator unseal` command or the
API before the server can respond to requests.

For more information, please refer to:

- [`operator init` command](/vault/docs/commands/operator/init) for information
  on initializing a Vault server.

- [`operator unseal` command](/vault/docs/commands/operator/unseal) for
  information on providing unseal keys.

- [Vault configuration](/vault/docs/configuration) for the syntax and
  various configuration options for a Vault server.

## Command arguments

None.

## Command options

None.

## Command flags

<br />

@include 'cli/server/flags/config.mdx'

<br /><hr /><br />

@include 'cli/server/flags/exit-on-core-shutdown.mdx'

<br /><hr /><br />

@include 'cli/server/flags/experiment.mdx'

<br /><hr /><br />

@include 'cli/server/flags/log-file.mdx'

<br /><hr /><br />

@include 'cli/server/flags/log-format.mdx'

<br /><hr /><br />

@include 'cli/server/flags/log-level.mdx'

<br /><hr /><br />

@include 'cli/server/flags/log-rotate-bytes.mdx'

<br /><hr /><br />

@include 'cli/server/flags/log-rotate-duration.mdx'

<br /><hr /><br />

@include 'cli/server/flags/log-rotate-max-files.mdx'

<br /><hr /><br />

@include 'cli/server/flags/recovery.mdx'

<br /><hr /><br />

@include 'cli/server/flags/dev.mdx'

<br /><hr /><br />

@include 'cli/server/flags/dev-cluster-json.mdx'

<br /><hr /><br />

@include 'cli/server/flags/dev-listen-address.mdx'

<br /><hr /><br />

@include 'cli/server/flags/dev-no-store-token.mdx'

<br /><hr /><br />

@include 'cli/server/flags/dev-root-token-id.mdx'

<br /><hr /><br />

@include 'cli/server/flags/dev-tls.mdx'

<br /><hr /><br />

@include 'cli/server/flags/dev-tls-cert-dir.mdx'

<br /><hr /><br />

@include 'cli/server/flags/dev-tls-san.mdx'

<br /><hr /><br />

## Global flags

<br />

@include 'cli/standard-settings/all-standard-flags.mdx'

## Examples

Start a server with a configuration file:

```shell-session
$ vault server -config=/etc/vault/config.hcl
```

Run in "dev" mode with a custom initial root token:

```shell-session
$ vault server -dev -dev-root-token-id="root"
```