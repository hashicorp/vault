---
layout: api
page_title: Azure plugin API - DeleteConfiguration
description: >-
  BLAH
---

# DeleteConfiguration endpoint

Delete the saved plugin configuration and associated credentials.

| Method   | Path                                 |
| -------- | ------------------------------------ |
| `DELETE` | `/{mount_path}/{plugin_name}/config` |

Required permissions: **TBD**

## Path parameters

<br />

@include 'plugin-api-fields/general/path/mount_path.mdx'

<br /><hr /><br />

@include 'plugin-api-fields/general/path/plugin_name.mdx'

Default name: `azure`.

## Request object

### Request body

The endpoint does not expect request data.

### Example request

<CodeTabs>

```shell-session
$ curl                            \
    --header "X-Vault-Token: ..." \
    --request DELETE              \
    https://${VAULT_ADDR}/v1/${MOUNT_PATH}/azure/config
    
```

```go
TBD
```

</CodeTabs>

## Response object

The plugin returns `200 OK` on success and `500 Internal Server Error` on
failure.