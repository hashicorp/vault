### Generated by 'make ci-config' do not manually edit this file.
version: 2
jobs:
  zip-make-test-race-samples:
    docker:
    - image: golang:1.12.4-stretch
    shell: /usr/bin/env bash -euo pipefail
    working_directory: /src
    steps:
    - attach_workspace:
        at: /tmp/workspace
    - run:
        command: |
          cd /tmp/workspace
          SHORT_SHA=$(cut -c1-8 <<< $CIRCLE_SHA1)
          TARBALL=sample-${SHORT_SHA}-$(date +%Y-%m-%dT%H_%M).tar.gz
          # Write TARBALL containing sample/make-test-race to zipped/sample/make-test-race
          OUTDIR=zipped/sample/make-test-race
          mkdir -p $OUTDIR
          tar -czf $OUTDIR/$TARBALL sample/make-test-race
          tree /tmp/workspace
        name: zip up samples
    - store_artifacts:
        path: /tmp/workspace/zipped/sample/make-test-race
  install-ui-dependencies:
    docker:
    - image: node:10-stretch
    shell: /usr/bin/env bash -euo pipefail
    working_directory: /src
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - run:
        command: |
          set -eux -o pipefail

          cd ui
          yarn install --ignore-optional
          npm rebuild node-sass
        name: Install UI dependencies
    - save_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
        paths:
        - ui/node_modules
  test-ui:
    docker:
    - image: node:10-stretch
    shell: /usr/bin/env bash -euo pipefail
    working_directory: /src
    resource_class: medium+
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          set -eux -o pipefail

          # Install Chrome
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub \
            | apt-key add -
          echo "deb http://dl.google.com/linux/chrome/deb/ stable main" \
            | tee /etc/apt/sources.list.d/google-chrome.list
          apt-get update
          apt-get -y install google-chrome-stable
          rm /etc/apt/sources.list.d/google-chrome.list
          rm -rf /var/lib/apt/lists/* /var/cache/apt/*

          # Add ./bin to the PATH so vault binary can be run by Ember tests
          export PATH="${PWD}/bin:${PATH}"

          # Run Ember tests
          cd ui
          mkdir -p test-results/qunit
          yarn run test-oss
        name: Test UI
    - store_artifacts:
        path: ui/test-results
    - store_test_results:
        path: ui/test-results
  build-go-dev:
    docker:
    - image: golang:1.12.4-stretch
    shell: /usr/bin/env bash -euo pipefail
    working_directory: /src
    steps:
    - checkout
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          set -eux -o pipefail

          # Move dev UI assets to expected location
          rm -rf ./pkg
          mkdir ./pkg

          # Build dev binary
          make bootstrap dev
        name: Build dev binary
    - persist_to_workspace:
        paths:
        - bin
        root: .
  test-go-race:
    machine: true
    shell: /usr/bin/env bash -euo pipefail
    working_directory: ~/src
    steps:
    - checkout
    - run:
        command: |
          set -eux -o pipefail

          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: |
          set -eux -o pipefail

          # Install Go
          curl -sSLO "https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz"
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
          rm -f "go${GO_VERSION}.linux-amd64.tar.gz"
          export GOPATH=/go
          export PATH="${PATH}:${GOPATH}/bin:/usr/local/go/bin"

          # Install CircleCI CLI
          curl -sSL \
              "https://github.com/CircleCI-Public/circleci-cli/releases/download/v${CIRCLECI_CLI_VERSION}/circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz \
              -C /usr/local/bin \
              "circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64/circleci"

          # Split Go tests by prior test times
          package_names=$(go list \
              -tags "${GO_TAGS}" \
              ./... \
            | grep -v /integ \
            | grep -v /vendor/ \
            | sort \
            | circleci tests split --split-by=timings --timings-type=classname)

          # Install gotestsum
          curl -sSL "https://github.com/gotestyourself/gotestsum/releases/download/v${GOTESTSUM_VERSION}/gotestsum_${GOTESTSUM_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz -C /usr/local/bin gotestsum

          # Run tests
          make prep
          mkdir -p test-results/go-test
          CGO_ENABLED= \
            VAULT_ADDR= \
            VAULT_TOKEN= \
            VAULT_DEV_ROOT_TOKEN_ID= \
            VAULT_ACC= \
            gotestsum --format=short-verbose --junitfile test-results/go-test/results.xml -- \
              -tags "${GO_TAGS}" \
              -timeout=40m \
              -parallel=20 \
              -race \
              ${package_names}
        name: Run Go tests
        no_output_timeout: 20m
    - store_artifacts:
        path: test-results
    - store_test_results:
        path: test-results
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.4
    - GOTESTSUM_VERSION: 0.3.3
  test-go:
    machine: true
    shell: /usr/bin/env bash -euo pipefail
    working_directory: ~/src
    parallelism: 2
    steps:
    - checkout
    - run:
        command: |
          set -eux -o pipefail

          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: |
          set -eux -o pipefail

          # Install Go
          curl -sSLO "https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz"
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
          rm -f "go${GO_VERSION}.linux-amd64.tar.gz"
          export GOPATH=/go
          export PATH="${PATH}:${GOPATH}/bin:/usr/local/go/bin"

          # Install CircleCI CLI
          curl -sSL \
              "https://github.com/CircleCI-Public/circleci-cli/releases/download/v${CIRCLECI_CLI_VERSION}/circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz \
              -C /usr/local/bin \
              "circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64/circleci"

          # Split Go tests by prior test times
          package_names=$(go list \
              -tags "${GO_TAGS}" \
              ./... \
            | grep -v /integ \
            | grep -v /vendor/ \
            | sort \
            | circleci tests split --split-by=timings --timings-type=classname)

          # Install gotestsum
          curl -sSL "https://github.com/gotestyourself/gotestsum/releases/download/v${GOTESTSUM_VERSION}/gotestsum_${GOTESTSUM_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz -C /usr/local/bin gotestsum

          # Run tests
          make prep
          mkdir -p test-results/go-test
          CGO_ENABLED= \
            VAULT_ADDR= \
            VAULT_TOKEN= \
            VAULT_DEV_ROOT_TOKEN_ID= \
            VAULT_ACC= \
            gotestsum --format=short-verbose --junitfile test-results/go-test/results.xml -- \
              -tags "${GO_TAGS}" \
              -timeout=40m \
              -parallel=20 \
               \
              ${package_names}
        name: Run Go tests
        no_output_timeout: 20m
    - store_artifacts:
        path: test-results
    - store_test_results:
        path: test-results
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.4
    - GOTESTSUM_VERSION: 0.3.3
  sample-make-test:
    machine: true
    shell: /usr/bin/env bash -euo pipefail
    working_directory: ~/src
    parallelism: 1
    steps:
    - checkout
    - run:
        command: |
          set -eux -o pipefail
          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: "set -eux -o pipefail\n\n# Install Go\ncurl -sSLO \"https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz\"\
          \nsudo rm -rf /usr/local/go\nsudo tar -C /usr/local -xzf \"go${GO_VERSION}.linux-amd64.tar.gz\"\
          \nrm -f \"go${GO_VERSION}.linux-amd64.tar.gz\"\nexport GOPATH=/go\nexport\
          \ PATH=\"${PATH}:${GOPATH}/bin:/usr/local/go/bin\"\n\nOUTDIR=\"/tmp/workspace/$OUTDIR\"\
          \n\n# Run tests\nmkdir -p $OUTDIR\nOUT=\"$OUTDIR/CIRCLE_SHA1-testrun-$CIRCLE_NODE_INDEX.log\"\
          \n\n# We will force this to exit 0 each time, as this is treated as offline\n\
          # data gathering, not a blocking part of the pipeline.\n# This is also necessary\
          \ to have the persist_to_workspace step\n# executed unconditionally.\nset\
          \ +eo pipefail \nmake test 2>&1 | tee $OUT\n"
        environment:
          OUTDIR: sample/make-test
          TESTARGS: -v
        name: make test
        no_output_timeout: 20m
    - persist_to_workspace:
        paths:
        - sample/make-test
        root: /tmp/workspace
        when: always
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.4
    - GOTESTSUM_VERSION: 0.3.3
  sample-make-test-race:
    machine: true
    shell: /usr/bin/env bash -euo pipefail
    working_directory: ~/src
    parallelism: 1
    steps:
    - checkout
    - run:
        command: |
          set -eux -o pipefail
          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: "set -eux -o pipefail\n\n# Install Go\ncurl -sSLO \"https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz\"\
          \nsudo rm -rf /usr/local/go\nsudo tar -C /usr/local -xzf \"go${GO_VERSION}.linux-amd64.tar.gz\"\
          \nrm -f \"go${GO_VERSION}.linux-amd64.tar.gz\"\nexport GOPATH=/go\nexport\
          \ PATH=\"${PATH}:${GOPATH}/bin:/usr/local/go/bin\"\n\nOUTDIR=\"/tmp/workspace/$OUTDIR\"\
          \n\n# Run tests\nmkdir -p $OUTDIR\nOUT=\"$OUTDIR/CIRCLE_SHA1-testrun-$CIRCLE_NODE_INDEX.log\"\
          \n\n# We will force this to exit 0 each time, as this is treated as offline\n\
          # data gathering, not a blocking part of the pipeline.\n# This is also necessary\
          \ to have the persist_to_workspace step\n# executed unconditionally.\nset\
          \ +eo pipefail \nmake test 2>&1 | tee $OUT\n"
        environment:
          OUTDIR: sample/make-test-race
          TESTARGS: -v -race
        name: make test
        no_output_timeout: 20m
    - persist_to_workspace:
        paths:
        - sample/make-test-race
        root: /tmp/workspace
        when: always
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.4
    - GOTESTSUM_VERSION: 0.3.3
  go-mod-download:
    docker:
    - image: golang:1.12.4-stretch
    shell: /usr/bin/env bash -euo pipefail
    working_directory: /src
    steps:
    - add_ssh_keys:
        fingerprints:
        - c6:96:98:82:dc:04:6c:39:dd:ac:83:05:e3:15:1c:98
    - checkout
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: go mod download
        name: Download Go modules
    - run:
        command: go mod verify
        name: Verify checksums of Go modules
    - save_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
        paths:
        - /go/pkg/mod
  zip-make-test-samples:
    docker:
    - image: golang:1.12.4-stretch
    shell: /usr/bin/env bash -euo pipefail
    working_directory: /src
    steps:
    - attach_workspace:
        at: /tmp/workspace
    - run:
        command: |
          cd /tmp/workspace
          SHORT_SHA=$(cut -c1-8 <<< $CIRCLE_SHA1)
          TARBALL=sample-${SHORT_SHA}-$(date +%Y-%m-%dT%H_%M).tar.gz
          # Write TARBALL containing sample/make-race to zipped/sample/make-race
          OUTDIR=zipped/sample/make-race
          mkdir -p $OUTDIR
          tar -czf $OUTDIR/$TARBALL sample/make-race
          tree /tmp/workspace
        name: zip up samples
    - store_artifacts:
        path: /tmp/workspace/zipped/sample/make-race
workflows:
  ci:
    jobs:
    - install-ui-dependencies
    - go-mod-download
    - build-go-dev:
        requires:
        - go-mod-download
    - test-ui:
        requires:
        - install-ui-dependencies
        - build-go-dev
    - test-go:
        requires:
        - build-go-dev
    - test-go-race:
        requires:
        - build-go-dev
  testsample:
    jobs:
    - go-mod-download
    - sample-make-test:
        requires:
        - go-mod-download
    - zip-make-test-samples:
        requires:
        - sample-make-test
    - sample-make-test-race:
        requires:
        - go-mod-download
    - zip-make-test-race-samples:
        requires:
        - sample-make-test-race
  version: 2
