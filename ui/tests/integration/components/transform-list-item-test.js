import { module, todo } from 'qunit';
import { setupRenderingTest, test } from 'ember-qunit';
import { render } from '@ember/test-helpers';
import hbs from 'htmlbars-inline-precompile';

module('Integration | Component | transform-list-item', function(hooks) {
  setupRenderingTest(hooks);

  todo('it renders un-clickable item if no capabilities', async function(assert) {});

  todo('it is clickable with details menu item if read capability', async function(assert) {});

  todo('it has details and edit menu item if read & edit capabilities', async function(assert) {});

  todo('it is not clickable if built-in template', async function(assert) {});

  todo('it is not clickable if built-in alphabet', async function(assert) {});
});
