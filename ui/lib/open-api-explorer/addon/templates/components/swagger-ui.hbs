<PageHeader as |p|>
  <p.top>
  </p.top>
  <p.levelLeft>
    <NamespaceReminder as |R|>
      All requests will be executed with the request header <code>X-Vault-Namespace: {{R.namespace.path}}</code> though it is possible to use the namespace <code>{{R.namespace.path}}</code> as an API prefix.
      See <DocLink @path="/api/overview#namespaces">the API documentation</DocLink> for examples.
    </NamespaceReminder>
  </p.levelLeft>
</PageHeader>

<div id="{{this.elementId}}-swagger"></div>
