<Toolbar>
  <ToolbarActions>
    <Toggle
      @name='replication-refresh'
      @checked={{checkedValue}}
      @onChange={{action (mut checkedValue)}}>
        Auto-refresh {{if checkedValue 'on' 'off'}}
    </Toggle>
  </ToolbarActions>
</Toolbar>
