<SecretListHeader
  @model={{this.model}}
  @backendCrumb={{hash
    label=this.model.id
    text=this.model.id
    path="vault.cluster.secrets.backend.list-root"
    model=this.model.id
  }}
  @isEngine={{true}}
/>
<Toolbar>
  <ToolbarActions>
    <ToolbarLink @params={{array "configure"}}>
      Configure PKI
    </ToolbarLink>
  </ToolbarActions>
</Toolbar>

{{! ARG TODO: add condition for empty state }}
<EmptyState @title="PKI not configured" @message="This PKI mount hasn’t yet been configured with a certificate issuer.">
  <LinkTo @route="configure" @model={{this.model}}>
    Configure PKI
  </LinkTo>
</EmptyState>