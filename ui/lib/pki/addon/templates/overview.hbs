<SecretListHeader
  @model={{this.model}}
  @backendCrumb={{hash
    label=this.model.id
    text=this.model.id
    path="vault.cluster.secrets.backend.list-root"
    model=this.model.id
  }}
  @isEngine={{true}}
/>
<Toolbar>
  <ToolbarActions>
    {{! ARG TODO should go to configuration.create }}
    <ToolbarLink @params={{array "configuration"}}>
      Configure PKI
    </ToolbarLink>
  </ToolbarActions>
</Toolbar>

{{! ARG TODO: add condition for empty state }}
<EmptyState @title="PKI not configured" @message="This PKI mount hasn’t yet been configured with a certificate issuer.">
  {{! ARG TODO should go to configuration.create }}
  <LinkTo @route="configuration" @model={{this.model}}>
    Configure PKI
  </LinkTo>
</EmptyState>