import Component from '@glimmer/component';
import { tracked } from '@glimmer/tracking';

export default class PagePkiIssuerRotateRootComponent extends Component {
  @tracked title = 'Rotate root';
}
