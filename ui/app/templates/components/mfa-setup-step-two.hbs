<p>
  TOTP Multi-factor authentication (MFA) can be enabled here if it is required by your administrator. This will ensure that
  you are not prevented from logging into Vault in the future, once MFA is fully enforced.
</p>
<form id="mfa-setup-step-two" {{on "submit" this.submit}}>
  {{!-- <MessageError @errorMessage={{this.error}} class="has-top-margin-s" /> --}}
  <div class="field has-top-margin-l">
    <div>QR CODE HERE</div>
    <p>
      After scanning the code, enter the six-digit authentication code below.
    </p>
    <label class="is-label has-top-margin-s">
      Authentication code
    </label>
    <Input
      id="auth-code"
      name="auth-code"
      class="input"
      autocomplete="off"
      spellcheck="false"
      {{!-- @value={{constraint.passcode}} --}}
      {{!-- disabled={{or this.validate.isRunning this.newCodeDelay.isRunning}} --}}
      @value=""
    />
  </div>

  <div class="is-flex-start">
    <button
      id="continue"
      type="submit"
      {{! ARG TODO disable if no value in the input }}
      {{!-- disabled={{or this.validate.isRunning this.newCodeDelay.isRunning}} --}}
      class="button is-primary {{if this.validate.isRunning 'is-loading'}}"
    >
      Enable
    </button>
    <button
      id="cancel"
      type="button"
      {{! ARG TODO disable if no value in the input }}
      {{!-- disabled={{or this.validate.isRunning this.newCodeDelay.isRunning}} --}}
      class="button"
    >
      Cancel
    </button>
  </div>
</form>