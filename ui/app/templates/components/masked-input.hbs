<div class="masked-input {{if shouldObscure "masked"}}" data-test-masked-input>
	{{textarea class="input" rows=1 wrap="off" placeholder="value" value=value focus-in=(action "setFocus" true) focus-out=(action "setFocus" false) key-down=(action onKeyDown) change=(action onChange) data-test-textarea="true"}}	
	<button {{action "toggleMask"}}  class="masked-input-toggle button is-ghost is-compact" data-test-button>
    {{i-con glyph=(if isMasked "hidden" "visible") aria-hidden="true" size=16}}
	</button>
</div>
