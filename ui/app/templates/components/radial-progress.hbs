<circle
  class="path"
  cx="{{centerValue}}"
  cy="{{centerValue}}"
  r="{{r}}"
  fill="none"
  stroke-width="{{strokeWidth}}"
  />
<circle
  class="progress-fill"
  cx="{{centerValue}}"
  cy="{{centerValue}}"
  r="{{r}}"
  fill="none"
  stroke-width="{{strokeWidth}}"
  stroke-dasharray="{{c}}"
  stroke-dashoffset="{{dashArrayOffset}}"
  />
