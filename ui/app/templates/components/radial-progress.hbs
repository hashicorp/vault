<circle
  class="path"
  cx="{{centerValue}}"
  cy="{{centerValue}}"
  r="{{r}}"
  fill="none"
  stroke="#e6e6e6"
  stroke-width="{{strokeWidth}}"
  />
<circle
  class="progress-fill"
  cx="{{centerValue}}"
  cy="{{centerValue}}"
  r="{{r}}"
  fill="none"
  stroke="#f77a52"
  stroke-width="{{strokeWidth}}"
  stroke-dasharray="{{c}}"
  stroke-dashoffset="{{dashArrayOffset}}"
  />
