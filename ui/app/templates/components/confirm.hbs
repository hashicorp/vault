  <div class="confirm" ...attributes>
    {{yield (hash
      Trigger=(component "confirm/trigger")
      Message=(component "confirm/message"
        renderedTrigger=(readonly this.openTrigger)
        wormholeReference=this.wormholeReference
      )
      onTrigger=(action (mut this.openTrigger))
      onCancel=(action (mut this.openTrigger) '')
      )
    }}
  </div>
  <div id={{this.wormholeId}}>
  </div>

{{!-- the openTrigger property is on the parent component. Clicking the trigger mutates the state of the parent (Confirm) component. --}}
{{!-- rederedTrigger gets a read only copy of the openTrigger, which corresponds to the ID of the currently opened trigger --}}
