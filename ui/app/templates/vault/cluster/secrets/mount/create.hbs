{{! TODO out of scope, but this is a shared component between auth and secrets we should decouple }}

{{!-- <MountBackendForm @mountModel={{this.model}} @mountCategory="secret" @onMountSuccess={{action "onMountSuccess"}} /> --}}

<SecretEngine::MountCreate @mountModel={{this.model}} @mountType={{get this.router.currentRoute.params "mount_type"}} />