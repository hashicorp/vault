vault.cluster.secrets.mount.index here is where we need to show the list of mounts from the plugin/catelog endpoint we
should. use a component, but we're testing
<SecretEngine::MountSelect @mountsArray={{this.model}} />