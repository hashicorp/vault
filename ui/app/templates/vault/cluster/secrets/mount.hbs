{{!
  Copyright (c) HashiCorp, Inc.
  SPDX-License-Identifier: BUSL-1.1
}}
{{! ARG making new component that strips down the MOuntBackendForm component to secret only concerns, decoupling }}

{{! ARG TODO modify breadcrumbs when on create mount or tune? }}
<PageHeader as |p|>
  <p.top>
    <Page::Breadcrumbs @breadcrumbs={{this.breadcrumbs}} />
  </p.top>
  <p.levelLeft>
    <h1 class="title is-3" data-test-secret-mount={{this.model.secretEngineModel.backend}}>
      Enable a Secrets Engine
    </h1>
  </p.levelLeft>
</PageHeader>
{{! ARG TODO missing the line separator }}
{{#if (eq this.router.currentRout.localName "index")}}
  <SecretEngine::MountList @secretEngineModel={{this.model.secretEngineModel}} />
{{else}}
  create/edit/details
  {{outlet}}
{{/if}}