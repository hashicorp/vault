{{!-- {{outlet "pluginSettings"}} --}}
meep
{{!-- This template is used to display the plugin settings for a secret engine configuration. It is only shown for secret engines
that are configurable, such as AWS, SSH, Azure, and GCP. Engines like KVv1 and Cubbyhole do not have this tab or route.
vault.cluster.secrets.backend.configuration.plugin-settings.edit this is plugin settings details and edit view. move this
component here note: only isConfigurable engines will have this tab and route to show. so no kvv1, cubbyhole. but yes to aws,
ssh, azure, and gcp.
<SecretEngine::ConfigurationDetails @config={{this.model.config}} @typeDisplay={{this.typeDisplay}} @id={{this.modelId}} /> --}}