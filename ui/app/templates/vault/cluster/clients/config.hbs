<Toolbar>
  <ToolbarActions>
    {{#if @model.canEdit}}
      <LinkTo @route="vault.cluster.clients.edit" class="toolbar-link">
        Edit configuration
      </LinkTo>
    {{/if}}
  </ToolbarActions>
</Toolbar>

<Clients::Config @model={{@model}} />