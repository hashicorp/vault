<Toolbar>
  <ToolbarActions>
    <ToolbarLink @type="add" @params={{array "vault.cluster.access.oidc.providers.create"}}>
      Create provider
    </ToolbarLink>
  </ToolbarActions>
</Toolbar>

<Oidc::ProviderList @model={{this.model}} />