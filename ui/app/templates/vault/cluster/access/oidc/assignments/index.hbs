<Toolbar>
  <ToolbarActions>
    <ToolbarLink
      @type="add"
      @params={{array "vault.cluster.access.oidc.assignments.create"}}
      data-test-oidc-assignment-create
    >
      Create assignment
    </ToolbarLink>
  </ToolbarActions>
</Toolbar>

<Oidc::AssignmentList @model={{this.model}} />