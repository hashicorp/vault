import templateOnly from '@ember/component/template-only';

/**
 * @module BlockError
 * BlockError components are a wrapper around error messages
 *
 * @example
 * ```js
 * <BlockError >
 * content
 * </BlockError>
 * ```
 */

export default templateOnly();
