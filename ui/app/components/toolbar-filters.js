/**
 * @module ToolbarFilters
 * `ToolbarActions` components are containers for Toolbar filters and toggles.
 *
 * @example
 * ```js
 * <ToolbarFilters>Toolbar Filters</ToolbarFilters>
 * ```
 *
 */

import OuterHTML from './outer-html';

export default OuterHTML.extend({});
